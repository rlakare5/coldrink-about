// src/components/section/Services.jsx
import React from 'react';

const Services = () => (
  <section style={styles}>
    <h1>Services</h1>
    <p>From distribution to event partnerships — we serve fizz with style.</p>
  </section>
);

const styles = {
  minHeight: '100vh',
  padding: '4rem',
  backgroundColor: '#ff9800',
  color: 'white',
  textAlign: 'center',
};

export default Services;